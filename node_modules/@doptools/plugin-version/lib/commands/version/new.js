"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const VersionCommand_1 = require("./VersionCommand");
class VersionNew extends VersionCommand_1.VersionCommand {
    async run() {
        var _a;
        const { args, flags } = this.parse(VersionNew);
        const name = (_a = flags.name) !== null && _a !== void 0 ? _a : 'world';
        this.log(`hello ${name} from /home/vscode/devcontainer-generators/projects/plugin-version/src/commands/version/new.ts`);
        if (args.file && flags.force) {
            this.log(`you input --force and --file: ${args.file}`);
        }
    }
}
exports.default = VersionNew;
VersionNew.description = 'describe the command here';
VersionNew.flags = {
    help: command_1.flags.help({ char: 'h' }),
    // flag with a value (-n, --name=VALUE)
    name: command_1.flags.string({ char: 'n', description: 'name to print' }),
    // flag with no value (-f, --force)
    force: command_1.flags.boolean({ char: 'f' }),
};
VersionNew.args = [{ name: 'file' }];
