import { PackageJsonFile } from "@doptools/tslib-cli-core";
import Command from "@oclif/command";
export declare abstract class VersionCommand extends Command {
    protected packageJson: Promise<PackageJsonFile>;
    protected applyVersionToProject(version: string): Promise<void>;
}
