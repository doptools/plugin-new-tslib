import { Tree } from '@angular-devkit/schematics';
import { Class } from "type-fest";
declare const filePath: unique symbol;
declare const fileName: unique symbol;
declare const tree: unique symbol;
export declare class JsonFile<TData = {
    [key: string]: unknown;
}> {
    private [filePath];
    private [fileName];
    private [tree];
    constructor();
    constructor(data: TData);
    constructor(filename: string);
    constructor(filename: string, data: TData);
    constructor(tree: Tree);
    constructor(tree: Tree, data: TData);
    constructor(tree: Tree, filename: string);
    constructor(tree: Tree, filename: string, data: TData);
    loadFile(): Promise<this>;
    loadFileFrom(file: string): Promise<this>;
    saveFile(): Promise<void>;
    saveFileAs(file: string): this;
    clone(): this;
    toJSON(): TData;
    toString(): string;
    setFilePath(file: string): void;
    getFilePath(): string;
    private get __hasFilePath();
    private __applyData;
}
export declare function JsonFileType<T>(): Class<JsonFile<T> & T>;
export {};
