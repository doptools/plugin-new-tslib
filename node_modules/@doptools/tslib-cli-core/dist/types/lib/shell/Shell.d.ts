export interface IShellOptions {
    cwd?: string;
}
export declare class Shell {
    static exec(command: string, opts?: IShellOptions): number;
    static read(command: string, opts?: IShellOptions): string;
}
