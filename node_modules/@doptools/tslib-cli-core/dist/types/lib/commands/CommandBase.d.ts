import { Command } from '@oclif/command';
import { SimpleFs } from '../files/simple-filesystem/SimpleFs';
import { PackageJson } from "type-fest";
export declare abstract class CommandBase extends Command {
    protected fs: SimpleFs;
    protected readonly flags: {
        [key: string]: string | number | boolean | undefined;
    };
    protected readonly arguments: {
        [key: string]: string | number | boolean | undefined;
    };
    readonly verbose: boolean;
    get packageJson(): Promise<PackageJson | null>;
}
