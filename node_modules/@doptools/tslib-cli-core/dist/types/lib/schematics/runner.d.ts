/**
 * derived from https://github.com/angular/angular-cli/blob/master/packages/angular_devkit/schematics_cli/bin/schematics.ts
 */
import { Rule } from '@angular-devkit/schematics';
export interface IRuleOptions {
    dryRun: boolean;
    force: boolean;
    verbose: boolean;
    interactive: boolean;
    cwd: string;
    mode: 'rw' | 'ro';
}
export interface ISchematicOptions extends IRuleOptions {
    allowPrivate: boolean;
}
export declare class RuleRunner {
    private readonly _runner;
    private readonly _options;
    static create(options?: Partial<IRuleOptions>): RuleRunner;
    private constructor();
    execute(rules: Rule[]): Promise<boolean>;
}
export declare class ContextIsReadonlyError extends Error {
}
export declare class SchematicRunner {
    private readonly _path;
    static create(path: string, options?: Partial<ISchematicOptions>): SchematicRunner;
    private readonly _collectionName;
    private readonly _schematicName;
    private readonly _options;
    private readonly _logger;
    private constructor();
    execute(options: object): Promise<boolean>;
    private createWorkflowRun;
}
